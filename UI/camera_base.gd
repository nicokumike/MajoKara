extends Camera2D

func _input(event):
	if event is InputEventMouseButton:
		if event.is_pressed():
			pass
			# zoom in
			#if event.button_index == zoom_in:
				#zoom_pos = get_global_mouse_position()
				# call the zoom function
			# zoom out
			#if event.button_index == zoom_out:
				#zoom_pos = get_global_mouse_position()
				# call the zoom function
